Epitech tek 1 project : my_select

To see the HTML docs file execute ./my_docs

The sources are in the src repository

The include are in the include repository

You can use alias in changing the .alias file in the current alias

Syntaxe : - alias [cmd]=[new alias]
	  - the .alias should allways bee ending by a \n

if you don't want to use alias just remove the .alias
